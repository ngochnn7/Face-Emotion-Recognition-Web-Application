Face Emotion Recognition Web Application
(Using Classical Machine Learning Models)

📌 Tổng quan dự án
Dự án nhằm xây dựng một ứng dụng web nhận diện cảm xúc khuôn mặt từ hình ảnh dựa trên các mô hình học máy truyền thống.
Hệ thống cho phép người dùng tải ảnh khuôn mặt, thực hiện tiền xử lý và trích xuất đặc trưng, sau đó phân loại cảm xúc bằng nhiều mô hình khác nhau.

Dự án tập trung vào việc so sánh hiệu quả của các mô hình học máy trong cùng một bài toán, từ đó đánh giá ưu điểm và hạn chế của từng phương pháp.

🎯 Mục tiêu
Xây dựng ứng dụng web nhận diện cảm xúc khuôn mặt

Ứng dụng các mô hình học máy truyền thống:

Naive Bayes

K-Nearest Neighbors (KNN)

Logistic Regression

Random Forest

Support Vector Machine (SVM)

So sánh hiệu quả các mô hình trên cùng một tập dữ liệu

Thiết kế giao diện trực quan, thân thiện với người dùng

🤖 Mô hình sử dụng
Mô hình	Mô tả
Naive Bayes	Mô hình xác suất dựa trên giả định độc lập giữa các đặc trưng
K-Nearest Neighbors (KNN)	Phân loại dựa trên khoảng cách giữa các mẫu dữ liệu
Logistic Regression	Mô hình phân loại tuyến tính
Random Forest	Tập hợp nhiều cây quyết định nhằm tăng độ chính xác
Support Vector Machine (SVM)	Phân loại bằng cách tìm siêu phẳng tối ưu

🛠️ Công nghệ sử dụng
🔹 Frontend & Backend
HTML

CSS

JavaScript

Flask (Web Framework)

🔹 Machine Learning & Xử lý ảnh
Python

scikit-learn

NumPy

OpenCV (xử lý và phát hiện khuôn mặt)

⚙️ Chức năng chính
Tải ảnh khuôn mặt từ thiết bị người dùng

Tiền xử lý ảnh và trích xuất đặc trưng

Lựa chọn mô hình học máy để dự đoán cảm xúc

Hiển thị kết quả phân loại cảm xúc

So sánh kết quả giữa các mô hình khác nhau
